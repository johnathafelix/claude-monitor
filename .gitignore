# macOS
.DS_Store

# Local Claude Code / tooling state (machine-specific, not shared)
.claude/settings.local.json
.code-review-graph/
tasks/

# Local mongo backups (mongodump output, machine-specific)
mongo-backup-*/

# Editor
.vscode/
